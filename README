README

Textchange

A website through which students of a university can connect to buy/sell used textbooks.

Requirements: Node

To build/run:
unzip textchange-master.zip
cd into /textchange
run 'node loader.js' to create sql tables
run 'node index.js' to start server

To use website:
start server, then navigate to 'localhost:8080'
